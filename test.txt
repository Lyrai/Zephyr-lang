class BaseClass:
    string name
    
    string Name():
        return this.name

class DerivedClass < BaseClass:
    DerivedClass ff
    string str
    string Get():
        print this
        return this.str
        
    DerivedClass AddString(string s):
        this.str = this.str + s
        return this
    void init():
        this.str = ""
  
DerivedClass ss = DerivedClass()
ss.AddString("test string to add")
ss.name = "test"
print ss.Name()
print ss.str
double s
    
int fib(int n):
    if n <= 1
        return 0;
    if n == 2
        return 1;
        
    if n == 3
        return 1;
    return fib(n - 1) + fib(n - 2)
    
function sum():
    int ss(int a):
        return a + 5
    return ss
void printFunc(function a):
    print a(555)

print "fib(10) = " + fib(10)

function b = sum()
print b
printFunc(b)